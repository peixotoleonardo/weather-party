import { SpotifyConfig } from '@weather-party/config/spotify.config';
import { OpenWeatherConfig } from '@weather-party/config/open-weather.config';

export const configs = [OpenWeatherConfig, SpotifyConfig];
